@model MVC_Stok_Takip_Sistemi.Models.Entity.Unit
@{
    ViewBag.Title = "Add";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Add Unit</h2>

<div class="container">
    @using (Html.BeginForm("Save", "Unit", FormMethod.Post))
    {
        <div class="form-group">
            @*@Html.HiddenFor(x => x.ID)*@
            @Html.LabelFor(x => x.Unit1)
            @Html.TextBoxFor(x => x.Unit1, new { @class = "form-control" })
        </div>
        <div class="form-group">
            @Html.LabelFor(x => x.Description)
            @Html.TextAreaFor(x => x.Description, new { @class = "form-control" })
        </div>
        <div class="form-group d-flex justify-content-start" style="margin: 20px; text-align: left;">
            <button class="btn btn-success"> Add New Unit</button>
        </div>
    }
</div>